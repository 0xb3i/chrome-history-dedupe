{
  "name": "chrome-history-dedupe-extension",
  "version": "0.3.10",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:syntax": "node --check src/page-identity.js && node --check src/history-utils.js && node --check src/history-data.js && node --check src/navigation-tracker.js && node --check src/history-page.js && node --check src/storage.js && node --check src/background.js && node --check src/rename-page.js && node --check src/rename-overlay.js",
    "check:manifest": "node -e \"JSON.parse(require('node:fs').readFileSync('manifest.json', 'utf8')); console.log('manifest json ok')\"",
    "verify": "npm test && npm run check:syntax && npm run check:manifest"
  }
}
